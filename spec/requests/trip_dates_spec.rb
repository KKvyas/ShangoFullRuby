require 'spec_helper'

describe "TripDates" do
  describe "GET /trip_dates" do
    it "works! (now write some real specs)" do
      get trip_dates_path
      expect(response.status).to be(200)
    end
  end
end
