require 'spec_helper'

describe "Routes" do
  describe "GET /routes" do
    it "works! (now write some real specs)" do
      get routes_path
      expect(response.status).to be(200)
    end
  end
end
