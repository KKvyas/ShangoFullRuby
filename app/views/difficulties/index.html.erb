<section class="scaffold">

<h1>Listing difficulties</h1>

<p><%= link_to '+ New difficulty', new_difficulty_path %></p>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @difficulties.each do |difficulty| %>
      <tr>
        <td><%= difficulty.description %></td>
        <td><%= link_to 'Show', difficulty %></td>
        <td><%= link_to 'Edit', edit_difficulty_path(difficulty) %></td>
        <td><%= link_to 'Destroy', difficulty, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

</section>